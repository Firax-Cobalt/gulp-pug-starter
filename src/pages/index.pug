extends ../layouts/default

block variables 
  - var title = 'Главная страница'
  - var includeHomeLink = false
  - 
    var cardsData = []

block content
  include ../components/cards-list/cards-list
    block items
      include ../components/card/card
      if cardsData.length > 0
        +card(cardsData[0].image, cardsData[0].title, cardsData[0].text)
        +card(cardsData[1].image, cardsData[1].title, cardsData[1].text)
        +card(cardsData[2].image, cardsData[2].title, cardsData[2].text)
      else
        p карточек нет 😁